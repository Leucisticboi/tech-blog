<div class="mui-row">
  <div class="mui-col-sm-10 mui-col-sm-offset-1">
    <br>
    <br>
    {{#if loggedIn}}
      {{#each posts}}
      {{> blog-post}}
      {{/each}}
    {{else}}
      {{#with posts.[0]}}
      {{> blog-post}}
      {{/with}}
    {{/if}}
  </div>
</div>